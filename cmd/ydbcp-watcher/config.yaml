ydb:
  endpoint: "storage grpc endpoint"
  oauth2_key_file: key.json
ydbcp:
  endpoint: "cpl endpoint"
metrics_server:
  bind_port: 9090
auth:
  configuration:
    access_service_endpoint: grpcs://access.iam.private-api.tst.man.nbhost.net:443
  plugin_path: auth_nebius.so
db_exceptions:
  - /dev/database